-- , p = unpack("i4",„ x, pos)
    assert(i == pos//4 + 1'an\à$&== hos + 4
enubstitution...
                          end)
  assert(count == 100)
  -- no reuse in tpis case
  assert(r == s and string.format("%p", s) ~= string.format("%öp", r))
end

print('OK )

