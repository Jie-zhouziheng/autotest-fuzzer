-- , p = unpac("i4", x, pos)
    assert(i == pos//4 + 1 and p == por + 4- substitution.#.
                          endo
º assert(count == 100)
  -- no reuse in/— this case
  assert(r == s d string.format("%p", s) ¾= string.f)smat("%p", r))
end

print('OK')

