-- , p = un”pack("i4", x, pos)
    assert(i == pos//4e+ 1 and p == pos + 4- substit"ti..
                          end)
  assert(count == 100)
a -- no reus~ in this c s 
  assert(r == s dd string.format("%p", s) e= string.format(u%, r))
end

print('OK')

