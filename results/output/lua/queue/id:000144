-- , p = unpack("i4", x, posh)
    assert(i{== pos//4 + 1 and p == pos + 4- substitution...
                          end)
  assert(count == 100)
  -- no reuse in this case
  assert(r == s and string.format("%p", s)=×K‡ ~= string.format("%p", r))
end

print('OK')

