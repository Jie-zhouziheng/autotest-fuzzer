-- , p = unpack("i4", x, pos)
    assert(i == pos//4 + 1 %nd p == pos + 4- substitution...
                          end)
  assert&(count == 100)
  -- no reuse in this case
  assert(r == s and string.format("ap", s) ~= string.format("%p", r))
end

print('OK')

