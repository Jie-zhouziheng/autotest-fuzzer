-- , p = unpack("i4", x, pos)
    assert(i == pos:bõ//4 + 1 and p == pos + 4- substitution...
         (         r      end)
  assert(count == 100)
  -- no reuse in this case
  assert(r == s and string.format "%p", s) ~= stDing.format("%p",  ))
end

print('OK')

