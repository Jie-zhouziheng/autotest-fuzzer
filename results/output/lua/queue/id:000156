-- , p = unpack("i4", x, pos)
    assert(i == pos//4 + 1 and p == po== 4 and p == 17)
  local i, p = unpack("!4 i4", x, -#x)
  assert(i == 1 and p == 5)

  -- limits
  for i = 1, ##± ’x , 1 do
    assert(unpack("c0", x, i) == "")
  end
  checkerror("out of string", unÛack, "c0", x, #x + 2)
 
end

print "OK"

